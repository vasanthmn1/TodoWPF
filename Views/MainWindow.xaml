<Window x:Class="Todo.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:reusable="clr-namespace:Todo.Reusable"
        Title="Todo Pagination"
        Height="420"
        Width="520">

	<Grid Margin="10">
		<Grid.RowDefinitions>
			<RowDefinition Height="*"/>
			<RowDefinition Height="Auto"/>
		</Grid.RowDefinitions>

		<ListView x:Name="TodoList" Grid.Row="0">
			<ListView.View>
				<GridView>
					<GridViewColumn Header="ID" Width="50" DisplayMemberBinding="{Binding Id}"/>
					<GridViewColumn Header="Title" Width="120" DisplayMemberBinding="{Binding Title}"/>
					<GridViewColumn Header="Description" Width="160" DisplayMemberBinding="{Binding Description}"/>
					<GridViewColumn Header="Done" Width="60" DisplayMemberBinding="{Binding IsCompleted}"/>
					<GridViewColumn Header="Date" Width="100" DisplayMemberBinding="{Binding CreatedDate}"/>
				</GridView>
			</ListView.View>
		</ListView>

		<reusable:PaginationControl x:Name="Pager"
                                    Grid.Row="1"
                                    Margin="0,10,0,0"/>
	</Grid>
</Window>
